export type staffType = {
    name: string,
    position: string,
    pic: string,
};

export type ministryType = {
    name: string,
    description: string,
    pic: string,
};

export type contactFormType = {
    firstName: string,
    lastName: string,
    email: string,
    message: string,
}